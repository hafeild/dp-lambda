
<% if print_header %>
  <h3><span class="name"><%= tag.text %></span>

    <% if can_edit? and defined? vertical %>
      <%= form_for(example, method: :delete,
          url: vertical_tag_path(vertical, example), 
          html: {class: "inline"}) do |f| %>
        <button type="submit" class="btn btn-default btn-sm">
          <span class="glyphicon glyphicon-trash" aria-hidden="true"></span> 
          Disassociate</button>
      <% end %>
      <%= form_for(example, method: :get,
         url: edit_vertical_tag_path(vertical, tag),
         html: {class: "inline"}) do |f| %>
        <button type="submit" class="btn btn-default btn-sm">
          <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Edit
      </button>
      <% end %>
    <% end %>
  </h3>
<% end %>

<%= render(partial: "verticals_shared/related_vertical",
  locals: {title: "Associated assignments", vertical_from: Tag.new, 
           vertical_to: Assignment.new}) %>

<%= render(partial: "verticals_shared/related_vertical",
  locals: {title: "Associated analyses", vertical_from: Tag.new, 
           vertical_to: Analysis.new}) %>

<%= render(partial: "verticals_shared/related_vertical",
  locals: {title: "Associated software", vertical_from: Tag.new, 
           vertical_to: Software.new}) %>

<%= render(partial: "verticals_shared/related_vertical",
  locals: {title: "Associated datasets", vertical_from: Tag.new, 
           vertical_to: Dataset.new}) %>