<% provide(:title, "Editing assignment: #{@assignment.name}") %>
<%= render template: 'assignments/_modify_form', 
  locals: {action: assignment_group_assignment_path(@assignment.assignment_group, @assignment), method: :PATCH} %>