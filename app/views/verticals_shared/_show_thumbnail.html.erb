<%#
Generates the HTML for a show view.
Expects at least: 

  vertical -- Anything with a thubmnail field.
%>

<div class="thumbnail-wrapper show-view" 
  <% if vertical.thumbnail? %>
    style="background-image: url('<%= vertical.thumbnail.tile.url %>')">
    <%# image_tag(vertical.thumbnail.tile.url) %>
  <% else %>
    >
  <% end %>
</div>  