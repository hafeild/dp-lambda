<%#
Generates the HTML for an index view.
Expects at least: 

  vertical -- Anything with a thubmnail field.
%>

<div class="thumbnail-wrapper index-view">
  <% if vertical.thumbnail? %>
    <%= image_tag(vertical.thumbnail.tile.url) %>
  <% else %>
    <div class="empty"></div>
  <% end %>

</div>