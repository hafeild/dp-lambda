<li>
  <span class="vertical-name"
    ><%= link_to vertical_to.name, get_vertical_path(vertical_to) %></span> 
  <span class="snippet">
    &ndash; <%= get_snippet(vertical_to.summary, 200)%>
  </span>
  <% if can_edit? and editable and defined? vertical_from %>
    <div class="edit-toggle">
      <%= form_for(vertical_to, method: :get,
        url: edit_vertical_vertical_path(vertical_from, vertical_to),
        html: {class: "inline"}) do |f| %>
        <button type="submit" class="btn btn-light btn-sm" data-toggle="tooltip" title="Edit">
          <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
      </button>
      <% end %>
	  <%= form_for(vertical_to, method: :delete,
          url: vertical_vertical_path(vertical_from, vertical_to), 
          html: {class: "inline"}) do |f| %>
        <button type="submit" class="btn btn-light btn-sm" data-toggle="tooltip" title="Disassociate">
          <span class="glyphicon glyphicon-trash" aria-hidden="true"></span> 
        </button>
      <% end %>
    </div>
  <% end %>
</li>