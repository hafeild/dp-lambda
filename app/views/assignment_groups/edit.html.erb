<% provide(:title, "Editing assignment group: #{@assignment_group.name}") %>
<%= render template: 'assignment_groups/_modify_form', 
  locals: {action: assignment_group_path(@assignment_group.id), method: :PATCH} %>